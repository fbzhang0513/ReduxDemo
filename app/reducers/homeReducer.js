/**
 * homeReducer
 */

import * as types from '../constants/actionTypes';

const initialState = {
    data: "",
};

export let homeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};